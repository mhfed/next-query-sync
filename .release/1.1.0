next-query-sync 1.1.0 one-shot release trigger

This file intentionally triggers .github/workflows/publish.yml when merged to main.
The workflow must publish npm successfully before it creates v1.1.0 and the GitHub Release.
